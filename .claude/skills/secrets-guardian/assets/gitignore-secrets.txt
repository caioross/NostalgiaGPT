# gitignore-secrets.txt
# Copie as linhas abaixo para o .gitignore do projeto.
# Estas entradas cobrem todos os segredos identificados no portfólio.

# ---------------------------------------------------------------------------
# Arquivos de variáveis de ambiente (NUNCA versionar com valores reais)
# ---------------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development.local
.env.test.local

# Manter .env.example no git (sem valores reais):
# !.env.example

# ---------------------------------------------------------------------------
# Android — Keystore e senhas (AulaLogger)
# ---------------------------------------------------------------------------
keystore.properties
*.jks
*.keystore
upload-keystore.jks
release.keystore

# ---------------------------------------------------------------------------
# Google OAuth / Service Accounts
# ---------------------------------------------------------------------------
client_secret*.json
credentials*.json
service_account*.json
ga_token.json
token.json
*_credentials.json

# ---------------------------------------------------------------------------
# Arquivos de configuração com segredos aggregados (ProjetosMapper)
# ---------------------------------------------------------------------------
credentials_config.js
db_config.js
secrets.js
secrets.ts
config.secret.*

# ---------------------------------------------------------------------------
# Chaves privadas e certificados
# ---------------------------------------------------------------------------
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
*.ppk

# ---------------------------------------------------------------------------
# Dados sensíveis / privacidade (DNA Explorer)
# ---------------------------------------------------------------------------
dna_caio/
*.vcf
*.fastq
*.bam
*.sam
biometric/
genetic_data/

# ---------------------------------------------------------------------------
# Cofres e arquivos de senha de gerenciadores
# ---------------------------------------------------------------------------
*.kdbx
*.1pux
*.agilekeychain

# ---------------------------------------------------------------------------
# Logs que podem conter tokens
# ---------------------------------------------------------------------------
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ---------------------------------------------------------------------------
# Builds e artefatos (nunca devem ser versionados)
# ---------------------------------------------------------------------------
node_modules/
.next/
out/
dist/
build/
app/build/
.gradle/
*.apk
*.aab
